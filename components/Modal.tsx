import React from "react";

interface ModalProps {
  title: string;
  confirmLabel: string;
  cancelLabel: string;
}

const Modal = ({ title, confirmLabel, cancelLabel }: ModalProps) => {
  return <div></div>;
};

export default Modal;
